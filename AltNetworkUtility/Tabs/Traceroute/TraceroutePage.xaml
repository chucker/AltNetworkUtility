<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:iconfontservices="clr-namespace:AltNetworkUtility.Services.IconFont"
    xmlns:local="clr-namespace:AltNetworkUtility.Tabs.Traceroute"
    xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
    x:Class="AltNetworkUtility.Tabs.Traceroute.TraceroutePage"
    Title="Traceroute"
    BindingContextChanged="ContentPage_BindingContextChanged">

    <ContentPage.BindingContext>
        <local:TraceroutePageViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackLayout Orientation="Horizontal">
                <Label VerticalOptions="Center">Host:</Label>
                <Editor WidthRequest="170" Text="{Binding Host}" />
                <Button Text="Run" Padding="2"
                        Command="{Binding DebufferedCommandViewModel.RunCommand}" />
            </StackLayout>

            <Editor Grid.Row="1" StyleClass="monospace"
                    Text="{Binding DebufferedCommandViewModel.Output}" />

            <maps:Map x:Name="Map" Grid.Row="2" />
        </Grid>
    </ContentPage.Content>
</ContentPage>
